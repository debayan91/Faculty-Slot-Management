
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all documents if the user is authenticated.
    // This is for development purposes to avoid permission errors.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
